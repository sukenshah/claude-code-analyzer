{
  "name": "@claude-analyzer/mcp",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "npm install && tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "npm run build && vitest run --coverage"
  },
  "dependencies": {
    "@claude-analyzer/analyzer": "*",
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  }
}
